#!/usr/bin/env bash
# Generate API documentation using Codox.
# Output: docs/api/index.html
set -euo pipefail
cd "$(dirname "$0")/.."
echo "Generating API docs..."
clj -T:build docs
echo "Done. Open docs/api/index.html"
