name = Log in as admin
description = Standard login flow

steps =
    Given I am on the login page
    When I fill in "Email" with "{email}"
    When I fill in "Password" with "{password}"
    When I click the "Log in" button
    Then I should see "Welcome, {email}"

name = Create valid user
description = Create a standard test user

steps =
    Given I am on the user creation page
    When I fill in "Name" with "Test User"
    When I fill in "Email" with "test@example.com"
    When I click the "Create" button
    Then I should see "User created successfully"

name = Navigate to dashboard
description = Simple navigation macro

steps =
    Given I am logged in
    When I click the "Dashboard" link
    Then I should be on the dashboard page