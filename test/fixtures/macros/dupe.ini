name = login as alice
description = First definition
steps =
  Given I am alice
  Then I should be logged in

name = login as alice
description = Duplicate definition (should error)
steps =
  Given I am also alice
  Then I should be logged in too
