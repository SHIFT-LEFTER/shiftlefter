;; ShiftLefter default verb glossary for :web interface type
;; Projects can extend or override via :glossaries config

{:type :web
 :verbs
 {:click {:desc "Click on an element"}
  :fill {:desc "Enter text into an input field"}
  :see {:desc "Assert element is visible"}
  :navigate {:desc "Navigate to a URL"}
  :submit {:desc "Submit a form"}
  :hover {:desc "Hover over an element"}
  :drag {:desc "Drag element to a target"}
  :select {:desc "Select option from dropdown"}
  :check {:desc "Check a checkbox"}
  :uncheck {:desc "Uncheck a checkbox"}
  :scroll {:desc "Scroll to element or position"}
  :wait {:desc "Wait for condition or element"}
  :clear {:desc "Clear input field contents"}
  :press {:desc "Press keyboard key or chord"}
  :upload {:desc "Upload file to input"}
  ;; Navigation (0.3.6)
  :navigate-back {:desc "Navigate backward in browser history"}
  :navigate-forward {:desc "Navigate forward in browser history"}
  :refresh {:desc "Reload the current page"}
  ;; Interaction (0.3.6)
  :doubleclick {:desc "Double-click on an element"}
  :rightclick {:desc "Right-click (context menu) on an element"}
  :move {:desc "Move mouse to an element"}
  :count {:desc "Count matching elements"}
  ;; Alert handling (0.3.6)
  :accept-alert {:desc "Accept (OK) an alert dialog"}
  :dismiss-alert {:desc "Dismiss (Cancel) an alert dialog"}
  ;; Window/frame management (0.3.6)
  :maximize {:desc "Maximize the browser window"}
  :resize {:desc "Set browser window dimensions"}
  :switch-window {:desc "Switch to another browser window or tab"}
  :switch-frame {:desc "Switch to an iframe or back to main frame"}}}
